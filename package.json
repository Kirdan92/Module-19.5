{
  "name": "gitsearch",
  "version": "1.0.0",
  "description": "git hub user search app",
  "main": "script.js",
  "scripts": {  
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel script.js --watch --out-file build.js",
    "scss": "node-sass . --watch --recursive --output css"
  },
  "author": "Pawel Butra",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-react": "^6.24.1",
    "node-sass": "^4.11.0"
  }
}
